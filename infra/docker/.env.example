# Shared
NODE_ENV=development

# Auth service
PORT=4001
HOST=0.0.0.0
LOG_LEVEL=info

# API gateway (use PORT=4000 when running api-gateway)
# PORT=4000

# Redis (used by docker-compose)
REDIS_URL=redis://localhost:6379
